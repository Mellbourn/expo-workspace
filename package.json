{
  "name": "expo-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "native": "cd apps/cool-app && npm start",
    "clean": "rm -rf node_modules && npm cache clean --force && npm i && npm run native -- --reset-cache"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.9.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
